# Symlinked to ~/.bashrc by set-links.sh on host 'server'.

REPO_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/../../.." && pwd)"
source "$REPO_DIR/common/bash/bashrc.common"
source "$REPO_DIR/hosts/server/bash/bashrc.local"
